# Half-Life 2 VR Standalone для Meta Quest

[English version](README.md)

Неофициальный бесплатный некоммерческий автономный VR-порт Half-Life 2 для Meta Quest 3 и Quest 3S. Стартовый релиз этого репозитория — 0.982; он поддерживает Half-Life 2, Lost Coast, Episode One и Episode Two. Оригинальные игры в комплект не входят: их необходимо легально приобрести и установить в Steam.

В репозиторий входят `HL2VR-Standalone-0.982.apk`, Windows- и Linux-сборщики `BUILD_GAME_CACHE.bat`, `BUILD_GAME_CACHE.sh`, каталог `tools/` и полный оверлей VR-совместимости `vr_game_resources/`. Поэтому для сборки кэша достаточно клонировать репозиторий. Архив релиза содержит только APK, сборщики, документацию и VR-ресурсы.

## Оригинальная VR-реализация

Весь код, отвечающий за взаимодействие с VR-миром, написан для этого проекта с нуля. Код, VR-логика и другие ресурсы PC VR-версии не используются. Единственное исключение — карты и модели: сборщик кэша берёт их из легально установленной пользователем копии игры; в этот репозиторий и его релизы они не входят.

## Что потребуется

- Meta Quest 3 или Quest 3S с включённым режимом разработчика;
- компьютер с Windows 10/11 или Linux;
- USB-кабель для передачи данных и разрешённая USB-отладка;
- Android SDK Platform Tools (`adb`) либо другая программа для sideload-установки;
- легальная Steam-установка Half-Life 2; для прохождения эпизодов установите также Episode One и Episode Two;
- APK версии 0.982 из корня репозитория или файлов релиза; сам репозиторий можно клонировать либо скачать автономный архив дистрибутива.

## Подробная установка

### 1. Соберите легальный игровой кэш

1. Клонируйте/скачайте репозиторий в доступную для записи папку либо распакуйте `HL2VR-Standalone-0.982.7z`. Файлы `BUILD_GAME_CACHE.*`, каталоги `tools/` и `vr_game_resources/` должны находиться рядом.
2. Из корня репозитория/дистрибутива запустите в Windows `BUILD_GAME_CACHE.bat`. В Linux выполните:

   ```bash
   bash BUILD_GAME_CACHE.sh
   ```

3. По запросу укажите корень Half-Life 2 Steam Legacy — папку, внутри которой находятся `hl2` и `platform`, например:

   ```text
   D:\SteamLibrary\steamapps\common\Half-Life 2
   ```

4. Если рядом с `hl2` установлены `lostcoast`, `episodic` и `ep2`, сборщик автоматически добавит их. Результат появится в `game_cache/srceng`.

Сборщик исключает EXE/DLL, сохранения, логи, пользовательские настройки и папку `custom` каждой кампании. Он объединяет вашу легальную установку игры с ресурсами VR-совместимости из дистрибутива. Python не требуется. Для Linux нужны Bash 4+ и GNU coreutils.

### 2. Установите APK

Подключите шлем, подтвердите в нём USB-отладку и проверьте соединение:

```bash
adb devices
adb install -r HL2VR-Standalone-0.982.apk
```

Ключ `-r` обновляет уже установленную версию без удаления данных приложения. Если Android сообщает о несовместимой подписи, сохраните важные данные, удалите старый пакет и установите APK заново.

### 3. Скопируйте игровой кэш

Не меняйте внутреннюю структуру папки `srceng`; скопируйте её целиком:

```bash
adb push game_cache/srceng /sdcard/
```

На шлеме должна получиться следующая структура:

```text
/sdcard/srceng/hl2/
/sdcard/srceng/platform/
/sdcard/srceng/lostcoast/  (только если установлен Lost Coast)
/sdcard/srceng/episodic/   (только если установлен Episode One)
/sdcard/srceng/ep2/        (только если установлен Episode Two)
```

Запустите **Half-Life 2 VR Standalone** из раздела неизвестных источников, выберите кампанию в лаунчере и начните игру.

## Управление

В таблице указана стандартная раскладка для правой ведущей руки. В лаунчере можно включить леворукий режим — действия оружейной руки зеркалируются.

| Кнопка Touch | Действие |
|---|---|
| Левый стик | Движение; направление вперёд следует горизонтальному повороту головы |
| Правый стик | Поворот/обзор; в лаунчере выбирается плавный или резкий поворот |
| Курок ведущей руки (по умолчанию правой) | Огонь; наведение и выбор в меню |
| Курок неведущей руки | Контекстное действие: огонь из стационарного орудия или ускорение багги при альтернативном управлении |
| Любой Grip | Захват/использование соответствующей рукой; Grip ведущей руки также выполняет специальные действия оружия |
| A | Прыжок; подтверждение в меню |
| B | Короткий/долгий выбор оружия; назад в меню |
| X | Перезарядка; выбросить магазин/боеприпас при физической перезарядке |
| Y | Фонарик |
| Нажатие левого стика | Включить или выключить бег |
| Нажатие правого стика | Включить или выключить приседание |
| Кнопка Menu на левом контроллере | Открыть или закрыть меню паузы |

### VR-взаимодействия

- **Выбор оружия:** короткое нажатие B переключает оружие; удерживайте B, наведите руку на сектор кругового/табличного селектора и отпустите для подтверждения. В леворуком режиме это действие переносится на Y.
- **Физическая перезарядка (по умолчанию):** для правой ведущей руки выбросьте магазин или боеприпас кнопкой X (для левой — A), свободной рукой достаньте новый боеприпас из-за плеча, установите его и при необходимости передёрните затвор либо цевьё. Режим перезарядки меняется в настройках лаунчера.
- **Двуручный хват:** поднесите свободную руку к цевью и удерживайте её Grip; отпустите Grip, чтобы разорвать хват.
- **Альтернативный огонь:** с оружием в ведущей руке дважды сожмите её Grip с интервалом не более 350 мс. У гравипушки Grip ведущей руки притягивает/отпускает предмет, а курок создаёт импульс.
- **Предметы и органы управления:** Grip начинает и продолжает взаимодействие именно той рукой, которой объект был захвачен. Поддерживаются удерживаемые рычаги и колёса.
- **Физические лестницы:** если опция включена, поднесите любую руку к перекладине, удерживайте Grip и тяните руку вниз. Можно поочерёдно менять руки; отпустите оба Grip для схода.
- **Монтировка:** физический урон от контакта включается отдельно. При включённой опции наносите удар взмахом; при выключенной сохраняется обычная атака курком.
- **Стационарные орудия:** возьмитесь за рукояти, стреляйте любым курком; отпустите Grip любой руки, чтобы выйти.
- **Транспорт:** альтернативная схема использует левый стик для газа/тормоза, правый — для поворота, а курок неведущей руки — для ускорения багги.
- **Румскейл-передвижение:** дополнительная опция, по умолчанию выключена; реальное горизонтальное движение тела добавляет виртуальное перемещение.

## Обновление

Новую версию APK можно установить командой `adb install -r`. Версия 0.982 один раз сбрасывает настройки лаунчера, игры, графики и VR к новым значениям по умолчанию; сохранения, установленные игровые данные и пользовательские моды остаются на месте. Если в примечаниях к релизу указано изменение VR-ресурсов, заново соберите и скопируйте кэш. Встроенная проверка обновлений также может скачать APK с постоянным именем и открыть системный установщик Android; подтвердите установку в системном окне.

## Решение проблем

- **`unauthorized` в `adb devices`:** наденьте шлем, подтвердите отпечаток компьютера и переподключите USB.
- **Подключено несколько устройств:** добавьте к команде `adb -s СЕРИЙНЫЙ_НОМЕР ...`; номер выводит `adb devices`.
- **Игра возвращается в лаунчер или не видит контент:** путь должен быть ровно `/sdcard/srceng/hl2`, а не `/sdcard/srceng/srceng/hl2`.
- **Кампания не отображается:** установите её в Steam, повторите сборку и проверьте наличие `lostcoast/gameinfo.txt`, `episodic/gameinfo.txt` либо `ep2/gameinfo.txt` в готовом кэше.
- **После обновления появились графические ошибки или сбой запуска:** соберите чистый кэш и замените `/sdcard/srceng`, не объединяя старые и новые каталоги вручную.

## Правовая информация

Проект не содержит коммерческих карт, VPK, озвучки, сохранений и других данных Half-Life 2. Он распространяется бесплатно для некоммерческого использования. Товарные знаки Half-Life, Steam, Source и Meta Quest принадлежат их владельцам.
